require('./test');

var assert = require("assert")
require("./assert-helper");

var ViewModel = require('../client/core/view-model');
var WebControlWrapper = require('../client/controls/web-control-wrapper');

describe("WebControlWrapper", function () 
{
    it("should xxx", function()
    {
        var webControlWrapper = new WebControlWrapper(null, null, null, null, 69);
        assert.equal(69, webControlWrapper.getControl());
    });
});