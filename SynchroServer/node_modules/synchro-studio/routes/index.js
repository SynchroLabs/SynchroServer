
/*
 * GET home page.
 */
var pkg = require('../package.json');

exports.index = function(synchroStudio, req, res, identity, applications)
{
    synchroStudio.render('index', { identity: identity, applications: applications, processVersion: process.version, packageVersion: pkg.version }, res);
};