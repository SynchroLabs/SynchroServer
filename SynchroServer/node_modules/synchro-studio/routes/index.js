
/*
 * GET home page.
 */
var pkg = require('../package.json');

exports.index = function(synchroStudio, req, res, applications){
  synchroStudio.render('index', { applications: applications, processVersion: process.version, packageVersion: pkg.version }, res);
};